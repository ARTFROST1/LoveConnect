Хочу реализовать полноценную, двустороннюю систему разрыва связи между пользователями в Telegram WebApp-приложении duoLove.

Сейчас при нажатии кнопки "Разорвать связь" на странице профиля:

Блок "В отношениях с..." исчезает — ✅ ОК

Но:

На главной странице остаётся шапка с аватарками (моей и партнёра) ❌

У партнёра ничего не меняется: он всё ещё видит меня как активного партнёра ❌

Из его профиля всё ещё можно перейти в мой профиль ❌

🔁 Что нужно реализовать:
✅ 1. Двусторонний разрыв связи
При разрыве у одного пользователя:

Партнёрская связь должна быть удалена и у второго пользователя.

Это можно реализовать через Telegram-бота, который получает запрос:

bash
Copy
Edit
POST /unlink-partner?user_id=123&partner_id=456
и отправляет обоим пользователям команду удалить друг друга из локальной базы (если используется polling или односторонняя связь — можно просто сообщить пользователю, чтобы он тоже удалил).

✅ 2. Обновление главной страницы после разрыва
После удаления партнёра:

В Home.tsx при !partner должна показываться уже существующая карточка:
"Добавьте партнёра, чтобы начать играть в мини-игры"

UI должен быть таким же, как при первом входе.

Можно вызвать refreshPartner() сразу после удаления и принудительно обновить usePartnerSync.

✅ 3. Блокировка доступа к чужому профилю после разрыва
После разрыва партнёр не должен видеть:

Кто был его партнёром

Переход в чужой профиль по profile/:partnerId должен:

Либо перенаправлять обратно на /profile

Либо показывать "Пользователь больше не состоит в паре"

✅ 4. UX-дополнения
При разрыве у второго пользователя можно отправить уведомление через Telegram-бота:

"Ваш партнёр разорвал связь. Вы можете выбрать нового."

На главной у второго пользователя также должен отображаться экран "Добавить партнёра"

⚙️ Технически это включает:
Удаление записи из таблицы partner у обоих

Вызов refreshPartner() сразу после действия

Проверку на partner == null в:

Home.tsx → отобразить экран добавления

Profile.tsx → скрыть "В отношениях с"

PartnerProfile.tsx → блокировать доступ, если связи больше нет