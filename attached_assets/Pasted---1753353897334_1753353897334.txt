Хочу устранить несколько критических проблем, связанных с отображением главной страницы, профиля и логикой поведения приложения после разрыва связи с партнёром в Telegram WebApp-приложении duoLove.

❌ Проблемы, которые нужно исправить:
1. Главная страница отображается даже после разрыва связи
После нажатия "Разорвать связь" партнёр продолжает отображаться на главной, как будто связь всё ещё установлена.

Это неверно: после разрыва главная страница не должна отображаться вообще, пока не выбран новый партнёр.

✅ Что нужно:
После подтверждения разрыва связи:

Удалить партнёра из локальной базы (partner = null)

Обновить состояние через refreshPartner()

При следующем рендере Home.tsx — если !partner, отображать уже существующий экран:

css
Copy
Edit
Добро пожаловать в duoLove!
Добавьте партнёра, чтобы начать играть в мини-игры вместе
[Добавить партнёра]
2. Блок «В отношениях с…» не пропадает из профиля
Даже после разрыва связи, в Profile.tsx продолжает отображаться блок с информацией о партнёре.

✅ Что нужно:
После разрыва: удалить данные партнёра, обновить состояние профиля.

В компоненте профиля добавить условие: если partner == null, блок "В отношениях с..." не отображается.

3. Переход в профиль партнёра после разрыва
Сейчас при попытке перейти в /profile/:partnerId после разрыва отображается сообщение «Профиль недоступен» — это правильно, но не должно быть возможности перейти в него вообще, если связи уже нет.

✅ Что нужно:
В PartnerProfile.tsx:

При монтировании компонента проверять: текущий пользователь связан с этим partnerId

Если связи нет — делать navigate("/profile") или показывать экран "Вы больше не связаны с этим партнёром"

Дополнительно: все ссылки на партнёра в UI (аватар, имя, кнопка в профиле) должны исчезнуть после разрыва.

🧠 Общее поведение после разрыва должно быть таким:
Главная страница переключается в режим «добавить партнёра»

В профиле исчезает блок "в отношениях с"

Переход к /profile/:partnerId блокируется

У обоих пользователей обновляется UI

Вся навигация и данные партнёрства удаляются на уровне состояния и локальной базы

